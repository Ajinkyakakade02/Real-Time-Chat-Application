# Real-Time-Chat-Application
A real-time chat application built using Java and Spring Boot that enables instant communication between multiple users. The application uses WebSocket with STOMP over SockJS for bidirectional messaging and a responsive web interface accessible on any device through a browser.
